[
  {
    "name": "/log shows empty state",
    "steps": [
      {
        "send": { "text": "/log" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "No moderation events yet." } }
        ]
      }
    ]
  },
  {
    "name": "menu:log button shows empty state",
    "steps": [
      {
        "send": { "callback": "menu:log" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "editMessageText", "payload": { "text": "No moderation events yet." } }
        ]
      }
    ]
  }
]
